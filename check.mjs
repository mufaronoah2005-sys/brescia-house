import fs from 'node:fs';import path from 'node:path';let errors=[];let count=0;for(const file of fs.readdirSync('dist',{recursive:true}).filter(f=>f.endsWith('.html'))){const html=fs.readFileSync(path.join('dist',file),'utf8');count++;if((html.match(/<h1[ >]/g)||[]).length!==1)errors.push(file+': expected one h1');for(const m of html.matchAll(/(?:href|src)="(\/[^"#]*)(#[^"]*)?"/g)){let dest=path.join('dist',m[1]);if(fs.existsSync(dest)&&fs.statSync(dest).isDirectory())dest=path.join(dest,'index.html');if(!fs.existsSync(dest))errors.push(file+': missing '+m[1]);else if(m[2]&&dest.endsWith('.html')&&!fs.readFileSync(dest,'utf8').includes('id="'+m[2].slice(1)+'"'))errors.push(file+': missing anchor '+m[0]);}}if(errors.length){console.error(errors);process.exit(1)}console.log(`Verified ${count} pages: internal links, anchors, assets and single H1.`);
